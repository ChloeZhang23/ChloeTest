# ChloeTest

## Sorting utility

This repository includes a small Python sorting helper in `sorting.py`.

```python
from sorting import sort_items

numbers = [5, 3, 8, 1, 2]
print(sort_items(numbers))  # [1, 2, 3, 5, 8]
```

Run the tests with:

```bash
python -m unittest
```
